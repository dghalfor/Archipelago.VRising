using APVRising;
using APVRising.Archipelago;
using APVRising.Utils;
using BepInEx.Logging;
using HarmonyLib;
using ProjectM;
using ProjectM.CastleBuilding;
using ProjectM.Network;
using Stunlock.Core;
using Unity.Collections;
using Unity.Entities;
using static ProjectM.ProgressionUtility;
using static VCF.Core.Basics.RoleCommands;

namespace APVRising.Hooks;

[HarmonyPatch]
public static class UnlockResearch
{
	// majority of this code adapted from VampireCommandFramework @ VCF.Core/Breadstone/ChatHook.cs
	[HarmonyPatch(typeof(UnlockResearchSystem), nameof(UnlockResearchSystem.UnlockProgression))]
    [HarmonyPrefix]
    public static bool Prefix(
    EntityManager entityManager,
    UpdateUnlockedJobData progressionJobData,
    PrefabGUID researchGuid,
    Entity user,
    EntityCommandBuffer commandBuffer,
    PrefabLookupMap prefabMapping,
    Entity progressionEntity,
    bool logOnDuplicate = true)
    {
        var name = DebugTool.GetPrefabName(researchGuid);
        Plugin.BepinLogger.LogInfo($"[AP] UnlockProgression: {DebugTool.GetPrefabName(researchGuid)}");
        return true;
    }

    //ResearchEntry is a gridSelectionEntry which is part of the research station
}

[HarmonyPatch]
public static class ProgressionUtilityPatch
{
    [HarmonyPatch(typeof(ProgressionUtility), nameof(ProgressionUtility.UpdateUnlockedJobData))]
    [HarmonyPrefix]
    public static void UpdateUnlockedJobDataPrefix()
    {
        Plugin.BepinLogger.LogInfo("[AP] ProgressionUtility.UpdateUnlockedJobData [PREFIX]");
    }

    [HarmonyPatch(typeof(ProgressionUtility), nameof(ProgressionUtility.UpdateUnlockedJobData))]
    [HarmonyPostfix]
    public static void UpdateUnlockedJobDataPostfix()
    {
        Plugin.BepinLogger.LogInfo("[AP] ProgressionUtility.UpdateUnlockedJobData [POSTFIX]");
    }

    [HarmonyPatch(typeof(ProgressionUtility), nameof(ProgressionUtility.UpdateUnlockedBuffers))]
    [HarmonyPrefix]
    public static void UpdateUnlockedBuffersPrefix()
    {
        Plugin.BepinLogger.LogInfo("[AP] ProgressionUtility.UpdateUnlockedBuffers [PREFIX]");
    }

    [HarmonyPatch(typeof(ProgressionUtility), nameof(ProgressionUtility.UpdateUnlockedBuffers))]
    [HarmonyPostfix]
    public static void UpdateUnlockedBuffersPostfix()
    {
        Plugin.BepinLogger.LogInfo("[AP] ProgressionUtility.UpdateUnlockedBuffers [POSTFIX]");
    }

    [HarmonyPatch(typeof(ProgressionUtility), nameof(ProgressionUtility.HasUnlockedRecipeForItem))]
    [HarmonyPrefix]
    public static void HasUnlockedRecipeForItemPrefix()
    {
        Plugin.BepinLogger.LogInfo("[AP] ProgressionUtility.HasUnlockedRecipeForItem [PREFIX]");
    }

    [HarmonyPatch(typeof(ProgressionUtility), nameof(ProgressionUtility.HasUnlockedRecipeForItem))]
    [HarmonyPostfix]
    public static void HasUnlockedRecipeForItemPostfix()
    {
        Plugin.BepinLogger.LogInfo("[AP] ProgressionUtility.HasUnlockedRecipeForItem [POSTFIX]");
    }

    [HarmonyPatch(typeof(ProgressionUtility), nameof(ProgressionUtility.GatherUnlockedRecipes))]
    [HarmonyPrefix]
    public static void GatherUnlockedRecipesPrefix()
    {
        Plugin.BepinLogger.LogInfo("[AP] ProgressionUtility.GatherUnlockedRecipes [PREFIX]");
    }

    [HarmonyPatch(typeof(ProgressionUtility), nameof(ProgressionUtility.GatherUnlockedRecipes))]
    [HarmonyPostfix]
    public static void GatherUnlockedRecipesPostfix()
    {
        Plugin.BepinLogger.LogInfo("[AP] ProgressionUtility.GatherUnlockedRecipes [POSTFIX]");
    }
}
